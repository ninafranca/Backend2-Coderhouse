<h1 id="title-productos" style="font-size: 170%; padding-top: 5%">Productos</h1>
<div class="div-products">
    <div class="div-div-products">
        {{#if products}}
        {{#each products}}
            <div class="products-title card shadow p-3 mb-5 bg-body rounded">
                <h2 class="products-title-title">{{this.title}}</h2>
                <h2 class="products-title-brand">{{this.brand}}</h2>
                <h2 class="products-title-description">{{this.description}}</h2>
                <h2 class="products-title-price">${{this.price}}</h2>
                <img src="{{this.thumbnail}}" alt="Product image" style="width: 10vw">
            </div>
        {{/each}}
        {{else}}
            <h2>No hay productos</h2>
        {{/if}}
    </div>
</div>